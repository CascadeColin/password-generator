const buttonStyle =
  "rounded p-1 bg-slate-600 hover:bg-slate-500 transition duration-200 mt-2";

const passwordHeaderStyle = "text-center mt-10 mb-1 text-2xl max-w-xl mx-auto";

const passwordContentStyle =
  "text-center mb-5 text-lg [word-wrap:break-word] h-32 max-w-xl mx-auto";

const formContainerStyle = "flex flex-col max-w-lg mx-auto";

const formHeaderStyle = "text-center mb-2 text-xl";

const formStyle = "flex flex-col items-center";

const formInputLengthStyle = "bg-white text-black h-[20px] w-11 ml-[10px] bg-none rounded-sm outline-none";

const formLabelStyle = "leading-7 mb-[10px]";

export const passwordGenStyle = {
    buttonStyle,
    passwordHeaderStyle,
    passwordContentStyle,
    formContainerStyle,
    formHeaderStyle,
    formStyle,
    formInputLengthStyle,
    formLabelStyle
}